import React from 'react';

// Cortiz aesthetic uses flat color, no particles.
// We return null or a simple div to maintain component tree integrity but remove visual noise.
const Background = () => {
    return null;
};

export default Background;
